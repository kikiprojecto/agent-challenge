name: Build and Deploy to Nosana

on:
  push:
    branches: 
      - main
  workflow_dispatch: {}

env:
  IMAGE_NAME: kikiprojecto/neurocoder-ai
  IMAGE_TAG: v1

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        run: echo "Image pushed successfully!"

  # Optional: Auto-deploy to Nosana
  # Uncomment this section if you want automatic deployment
  # deploy-to-nosana:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #
  #     - name: Install Nosana CLI
  #       run: npm install -g @nosana/cli
  #
  #     - name: Deploy to Nosana
  #       env:
  #         NOSANA_WALLET_KEY: ${{ secrets.NOSANA_WALLET_KEY }}
  #       run: |
  #         nosana job post \
  #           --file ./nos_job_def/nosana_mastra.json \
  #           --market nvidia-3090 \
  #           --timeout 30
